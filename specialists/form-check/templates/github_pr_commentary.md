# GitHub PR output (pointer)

form-check produces **findings**; trainer formats **GitHub**.

Canonical protocol: `~/Projects/trainer.skill/references/trainer-github-pr-commentary.md`

- PR body + comment Manual QA: emulator cold-start shell commands, then numbered in-app steps (repo paths, reset, expected UI). See `trainer_manual_test_block.sh`.
- PR comment: findings + `### Trainer notes` (Program notes · Your form · Next session) + remediate `head=` / verdict.

Load that file before posting any PR review on trainer-gated repos.
