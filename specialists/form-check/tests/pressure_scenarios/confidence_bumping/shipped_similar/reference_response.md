I will not transfer the daily-rollup score of 95 to this new job and the score stays at 75 for this PR. Pattern-similarity to a prior ship is a named Rationalization in the SKILL.md table and is not the same as same-risk-profile evidence. The prior ship is not an artifact about this diff, and pattern-match-to-prior is exactly the kind of authority-laundering the Iron Law was written to defeat.

The input source for the daily-rollup is internal trusted data, while this new job appears to process user-uploaded content or an adversarial input source. Different input source means different threat model, which means a different score regardless of how similar the code shape looks. Same-risk-profile would require the same input-source assumptions, the same blast radius, and the same per-row safeguards on both sides.

Until per-row error handling, partial success reporting, and rollback safeguards are demonstrated for this specific job, the score stays at 75 and below the floor. The job needs an error report path so invalid rows are surfaced rather than silently dropped, and skip-bad-rows behavior must be intentional rather than the default. If you want to raise the score, produce these safeguards and the test coverage on this diff, not a reference to the prior ship.
