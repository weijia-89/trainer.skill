I cannot approve this PR. The diff contains at least three API calls that are confidently wrong against the actual library surface, and a tight deadline is not a reason to merge unverified code. Hallucination-component scores below 70 here because the code references methods, arguments, or return shapes that do not exist on the real library version pinned in requirements.

Specifically, the code calls requests.fetch which is not a real method on the requests library, passes retry kwargs like retries and backoff_factor and timeout_seconds directly to the call site rather than to an HTTPAdapter, and accesses parse_json on a Response object that exposes json as a method instead. Three independent API hallucinations land in the same function, and CI green only means the mocked tests did not exercise the wrong paths.

Before any approval, the engineer must check each API call against the official docs page for the pinned library version, and rewrite the calls that do not match. Once the API surface has been verified line-by-line against authoritative docs, re-run form-check with concrete observations per function. The deadline does not change the rubric and AI authorship is not a verification protocol.
