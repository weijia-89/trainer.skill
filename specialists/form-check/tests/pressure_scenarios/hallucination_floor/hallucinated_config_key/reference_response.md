I cannot approve this PR even though the paging issue is real and the fix looks clean. The config keys pgbouncer_auto_failover and pg_pool_autoscale_max_connections and sqlalchemy_auto_reconnect_backoff do not appear in the pgbouncer docs or the sqlalchemy docs as real options, and config-key hallucinations are particularly insidious because env-var reads succeed silently while defaults fire and the feature does nothing. Hallucination-component score is below 70 until the keys are verified.

The engineer must demonstrate that each referenced config key actually exists in the official docs of the underlying tool, in the deployment config, in the secrets store, or in the documented environment of the running service. "It compiles and CI is green" is not evidence that the keys exist; CI passes because the defaults fired, not because the feature works. Verify each key name directly against the upstream documentation before any approval.

The legitimate alternative is to grep the deploy config and the runbook for the exact key names used in the PR, list the real alternative key names if the keys do not exist, and either rename the code to match reality or update the deploy config. Until the keys actually exist in the environment where this code will run, approval is theater and the paging incident will recur after merge.
