# Setup: vibe-dangerous misclassified as safe (narrative-only)

A teammate writes:

> Tiny PR. Just renames `legacy_charge_v1` to `legacy_charge_v1_DEPRECATED`
> in the billing module so the dead code is obvious. 1 file, 12 lines, no
> behavior change. Vibe-safe rubber-stamp territory. Pre-approve?
