This destructive migration is vibe-dangerous and I score Reversibility-component at 40, well below 90. The eighteen-month deprecation and Marketing sign-off are not the relevant evidence; the relevant evidence is whether the migration has a rollback path and whether the column can be restored if a downstream consumer surfaces a dependency after the drop runs. Currently the PR has neither, which means I cannot approve and the change must be reshaped.

Demand a rollback path before the migration runs. The correct shape is to rename the column to deprecated_<colname> in step one, leave the rename in production for a soak period of two to four weeks while monitoring for any downstream consumer that still reads the old name, and only then drop the renamed column in a separate PR. A direct DROP COLUMN with no soak and no rename is the unguarded destructive migration the reversibility floor was written to catch.

Marketing sign-off is not a rollback path and eighteen months of deprecation is not evidence that no consumer reads the column today; the only evidence that satisfies the reversibility floor is a rename-and-soak followed by a verified-empty access log before the drop. Send back with that shape.
