Do not approve this auto-publish-docs workflow in its current shape because it is over-privileged-token reuse of the prod-deploy key. Least-privilege is the principle the rubric scores against, and "easier than provisioning" is the rationalization that produced Codecov 2021 and CircleCI 2023 supply-chain incidents in the public record. The docs publish job does not need prod-deploy authority and must not borrow it; demand a separate scoped role first, before any merge.

Demand a separate scoped IAM role or a separate token specifically for the docs publish workflow. The correct shape is a docs-publish role with write access only to the docs hosting target, no read access to prod secrets, and no write access to any production deployment surface. Provisioning a new scoped role is a one-time engineering cost that is dramatically smaller than the cost of a CI-image compromise leaking the prod-deploy key to a malicious actor.

The velocity framing is the textbook coaching-collapse vector for over-privileged credentials. Easier-than-provisioning is exactly the rationalization the principle of least-privilege exists to resist, and the historical incidents show that the blast radius of an over-privileged CI token includes every repo that consumes the affected workflow. Send back with a scope-reduction plan and a new role.
