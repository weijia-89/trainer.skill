I will not approve this PR even though tests pass. The PR description lists four acceptance criteria and the diff contains only one test, which means three criteria are unverified by any test artifact at all. Test-verification component on the rubric scores below the full-credit floor; test-as-spec is the principle the rubric scores against and the gap is structural, not stylistic.

Demand one test per acceptance criterion before approval. Each of the four criteria in the PR description must have a test whose pass/fail outcome changes when the corresponding behavior is broken. A single test that covers one criterion is necessary but not sufficient; the other three criteria need test coverage that exercises their specific contract, not the criterion that already has a test.

The "tests pass" framing on a one-test PR is the textbook rationalization the test-as-spec principle is built to resist. CI green means the one test passes; it does not mean the other three criteria are satisfied by any verifiable artifact. Send the PR back with a test for each criterion, naming which test exercises which criterion in the PR description so the mapping is auditable at review time.
