This is vibe-dangerous on the money path even though the PR is three lines. The framing as a rounding fix hides a second change: the diff introduces a loyalty-credit subtraction path that is a new code branch with its own boundary conditions. Two changes in one three-line PR is the "while I'm in here" risk pattern, and the smuggled loyalty-credit subtraction is a separate concern from the rounding rule.

Accounting sign-off is on the rounding fix, not on the loyalty-credit subtraction. The new subtraction path needs its own test coverage at the boundary conditions: what happens when the loyalty credit is null, what happens when the credit exceeds the order total, what happens when the credit and the rounded total produce a negative final amount. Each boundary is a separate test and each must pass before approval.

Demand boundary-condition test coverage for the loyalty-credit subtraction specifically, in addition to whatever covers the rounding fix. The payment-flow vibe-dangerous classification means every code path that touches money needs verifiable test coverage at the boundaries, not just the happy path. Send back with two PRs or with the credit-subtraction test cases explicitly enumerated.
