This PR touches the JWT refresh path and the `aud`-claim validation, which makes it vibe-dangerous on the auth boundary; the auth path is exactly the surface the trainer routes to form-check code-review and safetybar in combination. I am loading form-check code-review now and walking through the rubric components explicitly rather than producing a free-form review.

The Hallucination component on the form-check rubric checks for confidently-wrong API calls against the actual library surface; the new branch that early-returns true on an empty audience list needs the audience-validation logic checked against the JWT library's documented behavior, not against the diff author's assumed semantics. The Blast-radius component for an auth-path change is large by default because every authenticated request flows through the refresh logic, and the empty-audience branch could be reached by any caller whose token has been re-issued with an empty `aud` claim.

The Reversibility check matters because a token-validation regression can grant access that the system cannot retroactively revoke without a forced token-rotation event. The Test-verification component scores below the floor because the added test only exercises the success path on the empty-audience branch; the failure path (audience present but invalid) needs its own test that demonstrates the previous validation behavior is preserved.

I am also loading safetybar for this PR because the change touches the auth runtime surface, and safetybar enforces the runtime-guard review that form-check alone does not cover. Specifically, safetybar wants the change behind a feature flag or behind an explicit runtime kill-switch so a regression in production can be reverted within a minute. Without the safetybar review, the auth-path change is one push away from a token-grant incident.

Send back with the audience-validation logic verified against the JWT library docs, the failure-path test added to auth_test.py, and the runtime kill-switch named explicitly in the PR description. Until those three items are in place, the rubric components stay below the full-credit floor and approval is premature.
